# GLOBAL_CTX — shared instructions for all fusioneer agents

- Be concise, file-precise, verification-driven.
- For hardware repos containing `fiducial/`, read `fiducial/AGENTS.md` and use `python fiducial/scripts/fiducial.py lint/check-intent/erc`.
- Verify after every change; do not claim clean without running the repo's `verify` command from `.opencode/fusioneer.json`.
