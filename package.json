{
  "name": "fusioneer",
  "description": "Home managed agents on opencode run — per-repo 24/7 agents, single VPS process",
  "version": "0.1.0",
  "license": "MIT",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run index.ts",
    "check": "tsc --noEmit",
    "fusioneer": "bun ./fusioneer"
  },
  "bin": {
    "fusioneer": "./fusioneer"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^7"
  },
  "dependencies": {
    "hono": "^4.13.5",
    "p-queue": "^9.3.3"
  }
}
